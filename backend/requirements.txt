# requirements.txt
# Install with: pip install -r requirements.txt

# Web framework (routes added in next phase)
fastapi>=0.111.0

# ASGI server
uvicorn[standard]>=0.29.0

# xAI Grok — via OpenAI-compatible SDK (just a base_url swap, no separate xAI package)
openai>=1.30.0

# Loads .env file into environment automatically
python-dotenv>=1.0.0

# Typing backports
typing-extensions>=4.11.0