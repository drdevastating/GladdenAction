# requirements.txt
# Install with: pip install -r requirements.txt

# Web framework (routes added in next phase)
fastapi>=0.111.0

# ASGI server
uvicorn[standard]>=0.29.0

# Groq SDK — official client for Groq's LPU inference API (Llama, Mixtral, etc.)
# Mirrors the OpenAI client interface; no extra adapters needed.
groq>=0.9.0

# Loads .env file into environment automatically
python-dotenv>=1.0.0

# Typing backports
typing-extensions>=4.11.0